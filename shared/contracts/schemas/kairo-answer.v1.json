{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kairo.local/contracts/kairo-answer.v1.json",
  "title": "KairoAnswerV1",
  "type": "object",
  "required": ["assessment", "claims"],
  "properties": {
    "assessment": { "type": "string", "minLength": 1 },
    "claims": { "type": "array", "items": { "$ref": "#/$defs/KairoClaimV1" } }
  },
  "$defs": {
    "KnowledgeScopeV1": {
      "type": "string",
      "enum": ["MANA_PRODUCTION", "PRODUCT", "PROJECT", "INCIDENT", "EXTERNAL_RESEARCH"]
    },
    "TemporalContextV1": {
      "type": "string",
      "enum": ["CURRENT", "TRANSITION", "FUTURE", "HISTORICAL"]
    },
    "EvidenceStateV1": {
      "type": "string",
      "enum": ["CONFIRMED", "OBSERVED", "PLANNED", "PROPOSED", "HYPOTHESIS", "VERIFY", "DEPRECATED", "CONTRADICTED"]
    },
    "KairoClaimV1": {
      "type": "object",
      "required": ["text", "scope", "temporalContext", "evidenceState", "evidenceRefs"],
      "properties": {
        "text": { "type": "string", "minLength": 1 },
        "scope": { "$ref": "#/$defs/KnowledgeScopeV1" },
        "temporalContext": { "$ref": "#/$defs/TemporalContextV1" },
        "evidenceState": { "$ref": "#/$defs/EvidenceStateV1" },
        "evidenceRefs": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
